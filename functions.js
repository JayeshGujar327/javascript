
// parameter and return type//
function greet(name) {
    return "hello: " + name;
}

let msg = greet("jay");

console.log(msg);

// return type with multiple parameters  // 
function add(a, b) {
    return a + b;
}

let result = add(10, 20);

console.log(result);